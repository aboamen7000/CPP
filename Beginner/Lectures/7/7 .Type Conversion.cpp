// Header file
#include <iostream>
// Type Conversion.
// Type Conversion : convert values from one type to another using type casting
using namespace std;
int main(){
    // 1- Implicit type conversion : convert values from one type to another
    // int x = 5;
    // float y = x; // implicit type conversion from int to float
    // cout << "Result of y : " << y << '\n';

    // 2- Explicit type conversion : convert values from one type to another
    //                              There are two types (static_cast / dynamic_cast).
    // double x = 5.5;
    // int y = static_cast<int>(x);
    // cout << x;


    // 3- Type conversion between built-in types : convert values between built-in types using arithmetic operations 
    // int x = 5;
    // float y = x / 2.0f;
    // cout<< y;

    // Continue conversion
    // int x = 5;
    // int y = 3;

    // double result = (double)x / y * 100; // 3.0 || 3.5
    // cout << result;

    //ASCII (American Standard Code for Information Interchange) is a 
    // character encoding standard used in computing. It assigns a unique numeric 
    // code to each of the 128 characters it supports, including uppercase and lowercase 
    // letters, numbers, punctuation, and control characters
    // Decimal Number == Integer (1,2,3,4.....etc)
    // char getting_the_char = 117;
    // cout << "Decimal Number 117 = (" << getting_the_char << ") Charactar Using ASCII" << endl;

    return 0; // function will end without any error
}