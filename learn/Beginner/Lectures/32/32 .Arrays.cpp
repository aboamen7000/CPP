// Header file
#include <iostream>
// Arrays.
// Arrays : a data structure consisting of a collection of elements (values or variables),
//                            each identified by at least one array index or key. 
using namespace std;

int main(){

    // Declaring and Initializing an Array
    // int numbers_list[5];

    // Method 1 : Assigns value x to the list element of the array
    // numbers_list[0]= 1;
    // numbers_list[1]= 2;
    // numbers_list[2]= 3;
    // numbers_list[3]= 4;
    // numbers_list[4]= 5;
    // cout << numbers_list[1] << endl;


    // Method 2 : Initializing an Array with a Range of Values
    // int numbers_list[100];
    // for(int i = 0; i < 100 ; i++){
    //     numbers_list[i] = i + 1;
    //     cout << numbers_list[i] << endl;
    // }
    

    // Method 3 : Accessing Array Elements with a Loop
    // float temp[7] = {22.0, 20.0, 19.0, 15.5, 6.0, 17.0, 18.0};
    // int the_size = sizeof(temp) / sizeof(float); // (4*6=) 24 / 4 = 6 || or  sizeof(temp) / sizeof(temp[0]) [which get first array item bytes size]
    // for(int i = 0; i < the_size; i++){
    //     cout << "Temperature on this day : " << i + 1 << " Was " << temp[i] << " degrees Celsius." << endl;
    // }


    
    return 0; // function will end without any error
}